# Changelog

## 1.0.0 — 2026-08-07

- First real release, distributed directly from the Aegis download centre.
- Full Authentication API coverage with Swift concurrency (`async`/`await`).
- Actor-isolated client, safe for concurrent use.
- Typed `AegisError` with network/auth/license classification.
- Background heartbeat task with revocation callback.